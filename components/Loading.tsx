import { Loader } from "lucide-react";
import React from "react";

const Loading = () => {
  return (
    <Loader className="animate-spin mx-auto text-center text-blue-500 my-10" />
  );
};

export default Loading;
